import { BaseModule } from '../../core/module/base-module';

/**
 * Пример модуля для демонстрации структуры и интеграции с ядром
 * Этот класс можно использовать как шаблон для создания новых модулей
 */
export class ExampleModule extends BaseModule {
  /**
   * Конструктор модуля
   */
  constructor() {
    super(
      'example-module', // id: уникальный идентификатор модуля
      'Example Module', // name: понятное название модуля
      '1.0.0', // version: версия модуля (семантическое версионирование)
      [] // dependencies: массив идентификаторов модулей-зависимостей
    );

    // Дополнительные свойства для проверки совместимости с ядром (опционально)
    (this as any).minCoreVersion = '1.0.0'; // Минимальная совместимая версия ядра
    (this as any).maxCoreVersion = '1.9.9'; // Максимальная совместимая версия ядра
  }

  /**
   * Метод инициализации модуля
   * Вызывается при запуске приложения после загрузки всех зависимостей
   * Используйте этот метод для настройки модуля, подключения к ресурсам,
   * загрузки конфигурации и т.д.
   */
  protected async onInitialize(): Promise<void> {
    // Пример логики инициализации:
    // - Загрузка конфигурации
    // - Подключение к базе данных
    // - Инициализация кэшей
    // - Загрузка необходимых данных

    // Здесь может быть асинхронный код
    await new Promise(resolve => setTimeout(resolve, 100));
  }

  /**
   * Метод запуска модуля
   * Вызывается после инициализации всех модулей
   * Используйте этот метод для запуска активных компонентов модуля,
   * например, воркеров, таймеров, слушателей событий и т.д.
   */
  protected async onStart(): Promise<void> {
    // Пример логики запуска:
    // - Запуск воркеров
    // - Запуск таймеров
    // - Запуск серверов
    // - Подписка на события

    // Имитация асинхронного запуска
    await new Promise(resolve => setTimeout(resolve, 100));
  }

  /**
   * Метод остановки модуля
   * Вызывается при остановке приложения
   * Используйте этот метод для корректного завершения работы модуля,
   * закрытия соединений, освобождения ресурсов и т.д.
   */
  protected async onStop(): Promise<void> {
    // Пример логики остановки:
    // - Остановка воркеров
    // - Отмена таймеров
    // - Закрытие соединений
    // - Сохранение состояния

    // Имитация асинхронной остановки
    await new Promise(resolve => setTimeout(resolve, 100));
  }

  /**
   * Метод проверки состояния модуля
   * Используется для мониторинга работоспособности модуля
   * @returns Статус работоспособности модуля (true - работает, false - не работает)
   */
  protected async onHealthCheck(): Promise<boolean> {
    // Пример логики проверки:
    // - Проверка соединений
    // - Проверка доступности ресурсов
    // - Проверка работы компонентов

    // В данном примере всегда возвращаем true
    return true;
  }

  /**
   * Публичные методы модуля
   * Реализуйте методы, которые будут использоваться другими модулями
   */
  public getExampleData(): string {
    return `Данные из модуля ${this.name}`;
  }

  /**
   * Внутренние методы модуля
   * Реализуйте вспомогательные методы для внутреннего использования
   */
  private processInternalData(data: any): any {
    return data;
  }
}

/**
 * Опционально: экспорт интерфейса модуля для типизации при использовании через ModuleRegistry
 */
export interface IExampleModule extends BaseModule {
  getExampleData(): string;
}
